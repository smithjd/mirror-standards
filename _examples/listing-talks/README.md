## listing-talks

A Quarto listing example that builds a table of talks from a YAML data file: each entry has a date, title, description, venue, and link.

Copy `talks.qmd` and `talks.yml` into your website root to use it.
