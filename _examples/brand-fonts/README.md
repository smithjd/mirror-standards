## brand-fonts

A `_brand.yml` example using two Google/Bunny fonts: one for body text, one for
headings (in the secondary color), plus underlined links.

Copy `_brand.yml` into your website root to use it.
