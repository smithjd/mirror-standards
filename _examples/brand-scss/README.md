## brand-scss

An example of using an `.scss` file to customize colors: set a brand `palette` color for the navbar foreground, `h1` headings, and lighten a brand color for the navbar title.

Copy `_brand.yml` and `styles.scss` into your website root.
