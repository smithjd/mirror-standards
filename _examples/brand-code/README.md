## brand-code

A `_brand.yml` example that adds code-font styling on top of the fonts example:
a monospace family, deep-purple inline code, and a light-blue code-block
background. 

Copy `_brand.yml` into your website root to use it.

Also copy `code-demo.qmd` if you want a page with code to demo the result.