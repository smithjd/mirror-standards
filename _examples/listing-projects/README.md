## listing-projects

A Quarto grid listing example built from `.qmd` pages in a `projects/` folder: each page sets a title, description, image, and categories in its metadata, and the listing pulls them into cards with a category index.

Copy `projects.qmd` and the `projects/` folder into your website root to use it. Demo photos are from Unsplash (credited on each page).
